ANTHROPIC_API_KEY=
GROQ_API_KEY=
ALLOWED_ORIGINS=http://localhost:8000,http://localhost:5175,http://localhost:3000
DATABASE_URL=postgresql+psycopg://resumebuddy:resumebuddy@localhost:5432/resumebuddy
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
AUTH_TOKEN_PEPPER=
PUBLIC_APP_URL=http://localhost:5175
# Keep true outside local HTTP development. Set false only for localhost HTTP.
SESSION_COOKIE_SECURE=true
SMTP_HOST=
SMTP_PORT=587
SMTP_STARTTLS=true
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM=
